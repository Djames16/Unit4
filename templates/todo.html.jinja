<!DOCTYPE html>
<html lang="en">
<head>
  <title>Woo Todo</title>

 <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
  <link rel="stylesheet" href="style.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css">
  <style>
    .complete {
      text-decoration: line-through;
      color: purple;
    }

    .btn{
      float: right;
    }

  </style>
</head>
<body class ="container mx-auto" style="background-color: purple;">
  <h1 class ="text-center" style="color: yellow;">Todo Application</h1>
  <ul class="card p-4" style="background-color: yellow;">
     {% for x in my_todos %}
      <li class="{%if x['complete']%}complete{%endif%}">
       {{x['description']}}
       <form method="POST" action="/delete_todo/{{x['id']}}" class="d-inline">
        <input type="submit" value="Delete" class="btn btn-danger">
       </form>
       {%if x['complete']%}🏁{%endif%}
       {%if x['complete']==0%}
       <form method="POST" action="/complete_todo/{{x['id']}}" class="d-inline">
        <input type="submit" value="Mark Complete" class="btn btn-success">
       </form>
       {%endif%}
      </li>
     {% endfor %}
</ul>
<form method="POST">
<input name="new_todo" placeholder="Try it out!">
<input type="submit">
</form>
</body>